<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Pogodeo.Mobile"
             x:Class="Pogodeo.Mobile.ShowWeatherPage"
             Title="{Binding Title}">

    <local:BasePage.ToolbarItems>
        <ToolbarItem Icon="icon_add" Command="{Binding AddNewPlaceCommand}" />
    </local:BasePage.ToolbarItems>

    <Grid>
        
        <Frame IsVisible="{Binding CityProvided, Converter={local:BooleanInvertConverter}}">
            <Label Text="Wybierz miasto dla którego chcesz sprawdzić pogodę" />
        </Frame>

        <FlexLayout IsVisible="{Binding CityProvided}"
                    x:Name="WeatherContainer">
            
        </FlexLayout>

    </Grid>

</local:BasePage>
